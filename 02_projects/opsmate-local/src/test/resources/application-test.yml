opsmate:
  security:
    requester-password: test-only-requester
    approver-password: test-only-approver
    buyer-password: test-only-buyer
    auditor-password: test-only-auditor
  llm:
    enabled: false

spring:
  jpa:
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    root: WARN
